# Copy this file to .env and fill in your values.
# .env is gitignored — never commit secrets.

# GitHub Personal Access Token — needed for the GitHub integration in Backstage.
# Generate one at https://github.com/settings/tokens (repo scope is sufficient).
GITHUB_TOKEN=ghp_your_token_here
